#pragma once
#include "../core/value.hpp"
#include <string>
#include <vector> 
#include <optional>
#include <chrono>

namespace miniRedis {
    
    class Istore {
    public:
        
        virtual ~Istore() = default;

    }
}